name: Deploy SmartGen Community frontend

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Validate Manus backend URL
        env:
          BACKEND_URL: ${{ secrets.VITE_API_BASE_URL || vars.VITE_API_BASE_URL }}
        run: |
          if [[ -z "$BACKEND_URL" ]]; then
            echo "::error::Add VITE_API_BASE_URL as a GitHub Actions secret or variable with the published Manus HTTPS backend URL."
            exit 1
          fi
          if [[ "$BACKEND_URL" != https://* ]]; then
            echo "::error::VITE_API_BASE_URL must be a public https:// backend URL, not a manus-webdev:// checkpoint URI or localhost URL."
            exit 1
          fi
      - name: Build frontend
        run: pnpm exec vite build
        env:
          VITE_BASE_PATH: /Smartgen-Platform-Community/
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL || vars.VITE_API_BASE_URL }}
          VITE_PUBLIC_APP_URL: https://jennymahmuda.github.io/Smartgen-Platform-Community/
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
